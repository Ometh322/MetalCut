# MetalCut — платформа продажи металлорежущего инструмента

Демо-версия маркетплейса: витрина + поиск с фасетными фильтрами + AI-подбор, кабинеты покупателя и селлера, админка. Рынок — RU. Демо должно вырасти в продукт без переписывания.

---

## 1. Зафиксированные решения

| Область | Решение |
|---|---|
| Фронтенд + бэкенд | Next.js (App Router) + TypeScript, full-stack в одном репо |
| Архитектура | Бизнес-логика в `src/services/*` (вне роутов и экшенов); роуты — только транспорт. Сервисы пишутся так, чтобы могли переехать в NestJS-процесс без переписывания |
| БД | PostgreSQL + Prisma ORM |
| Атрибуты товаров | JSONB в `Product.attributes`, структура и валидация — схема атрибутов категории (свой формат схемы на каждую категорию) |
| Поиск | Этап 0: Postgres FTS (tsvector/trigram) + JSONB-фильтры. Этап 1 (после демо): Meilisearch за интерфейсом `SearchEngine`, добавление драйвера без рефакторинга |
| AI-слой | LLM по OpenAI-совместимому API за интерфейсом `QueryParser`; задача — конвертировать естественный запрос в фильтры по схеме категории. Для демо — заглушка/правила, провайдер подключается ключом |
| Каталог | `Category` (дерево + схема атрибутов) → `Product` (карточка, атрибуты JSONB) → `Offer` (цена/остаток/срок от селлера). Разделены с первого дня |
| Импорт | CSV/Excel с маппингом колонок на атрибуты категории; формат обмена спроектирован под будущую 1С (CommerceML = ещё один источник тех же сущностей) |
| Аутентификация | Демо: email+пароль, JWT в httpOnly-cookie (jose), роли `ADMIN / SELLER / CUSTOMER` |
| Язык / рынок | RU, один язык (i18n не закладываем в демо) |
| Оплата | Нет. Корзина → «заказ-заявка» со статусами (заготовка под реальный заказ) |

## 2. UI-референс: каталог и поиск — как в DNS (dns.com.ru)

Прямых скринов нет — ориентируемся на паттерны DNS-маркетплейса. Что копируем:

- **Левая колонка фильтров**: сворачиваемые группы; в длинных списках значений — встроенный поиск по значениям; числовые диапазоны — два инпута (от/до) + бегунок; чекбоксы с количеством товаров у значения.
- **Чипсы применённых фильтров** над списком + «Сбросить всё».
- **Тулбар списка**: количество найденных товаров, сортировка (популярность/цена/новизна), переключатель вид сетка/список.
- **Режим «список» (главный для B2B)**: горизонтальные строки с ключевыми характеристиками прямо в строке (диаметр, материал, покрытие, хвостовик) — как в DNS.
- **Пагинация** постраничная + «Показать ещё».
- Липкая (sticky) панель фильтров при скролле.
- Карточка товара: галерея, таблица характеристик (из атрибутов), блок офферов селлеров (цена/срок/остаток), кнопка в корзину.

Общий стиль: нейтральный светлый UI, акцентный цвет — оранжево-жёлтый DNS-типа или индустриальный синий; шрифт системный/Inter. Уточняется при сборке UI-кита.

## 3. Структура репозитория

**Рабочий процесс:** по завершении каждой фазы (и промежуточных значимых кусков) — коммит **и push в origin** (github.com/Ometh322/MetalCut, ветка main). Не копим локальные коммиты.

```
prisma/                  — schema.prisma, seed, миграции
src/
  app/                   — только роуты (страницы и API)
    (shop)/              — витрина: /, /catalog/[...slug], /product/[slug], /cart, /checkout
    account/             — кабинет покупателя (заказы, повторный заказ)
    seller/              — кабинет селлера (товары, импорт, заказы)
    admin/               — админка (категории, схемы, модерация, селлеры)
    api/                 — REST-эндпоинты
  components/            — UI-компоненты (ui-kit + доменные)
  services/              — бизнес-логика: catalog, orders, offers, import, auth, search/, ai/
  lib/                   — db (prisma client), auth-утилиты, валидация
  data/                  — номенклатура: дерево категорий + схемы атрибутов (источник для seed и админки)
docker-compose.yml       — Postgres для локальной разработки
PLAN.md                  — этот файл (дополняется по мере работы)
```

## 4. Дорожная карта

### Фаза 0 — Фундамент ✅ (завершена 2026-09-15)
- [x] Каркас Next.js + TS + Tailwind, репо-структура
- [x] PLAN.md
- [x] Prisma-схема БД (User/Seller/Category/Product/Offer/Order)
- [x] Дерево категорий + схемы атрибутов (`src/data/nomenclature.ts`)
- [x] docker-compose с Postgres, .env, первичная миграция
- [x] Seed: 5 селлеров, 23 категории, 224 товара с реалистичной номенклатурой

### Фаза 1 — Витрина: каталог и фильтры ✅ (завершена 2026-09-15)
- [x] Дерево категорий (меню шапки, плитки на главной, страницы с проверкой цепочки slug)
- [x] Список товаров категории: режим список/сетка, сортировка (5 вариантов), пагинация
- [x] Фасетные фильтры по схеме категории (enum-чекбоксы со счётчиками, числовые диапазоны от/до, поиск по значениям, сворачиваемые группы)
- [x] Чипсы применённых фильтров, «Сбросить всё», счётчик результатов
- [x] Поиск по названию/артикулу/бренду (Postgres FTS websearch_to_tsquery + ILIKE; trigram — позже при необходимости)
- [x] Карточка товара: характеристики из схемы, таблица офферов селлеров (цена/наличие/срок/мин. партия), хлебные крошки
- [x] Главная: hero с поиском, статистика, плитки категорий, новинки
- [x] /catalog — все товары + глобальный поиск с фильтрами цена/бренд

### Фаза 2 — Корзина и заказ-заявка ✅ (завершена 2026-09-15)
- [x] Аутентификация: email+пароль (scrypt), JWT-сессия в httpOnly-cookie (jose), страницы /login, /register, guard кабинета
- [x] Корзина: localStorage-провайдер + обогащение актуальными ценами/остатками через POST /api/cart; группировка по продавцам, счётчики количества, автоудаление неактуальных позиций, бейдж в шапке
- [x] Кнопки «В корзину»: в строке/карточке каталога (лучший оффер), в блоке цены и в каждой строке таблицы офферов на карточке товара
- [x] Оформление заявки: форма контактов (префилл из профиля, работает и без регистрации), цены пересчитываются на сервере, номер вида MC-2026-NNNNNN, страница подтверждения
- [x] Кабинет покупателя /account: история заявок со статусами, детальная страница состава, «Повторить заявку» (наполняет корзину, актуальность проверяет корзина)
- [x] E2E в браузере: вход → корзина → заявка → кабинет → повтор → регистрация — всё работает

### Фаза 3 — Кабинет селлера
- [ ] Вход/регистрация селлера (модерация админом)
- [ ] CRUD карточек: форма из схемы атрибутов категории, черновик → на модерацию
- [ ] Цены/остатки/сроки по своим офферам
- [ ] Заказы по своим товарам
- [ ] Импорт CSV/Excel: загрузка файла, маппинг колонок на атрибуты, предпросмотр, массовое создание/обновление

### Фаза 4 — Админка
- [ ] Дерево категорий: CRUD, редактор схем атрибутов
- [ ] Модерация карточек (одобрить/отклонить с причиной)
- [ ] Управление селлерами и пользователями
- [ ] Просмотр заказов

### Фаза 5 — AI-поиск и рекомендации
- [ ] Интерфейс `QueryParser` + rule-based заглушка (разбор «концевая фреза 10 мм по нержавейке» → фильтры)
- [ ] Подключение LLM-провайдера (OpenAI-совместимый API, ключ в env)
- [ ] UI: строка AI-поиска, показ «понятых» фильтров с возможностью поправить
- [ ] Блок «Похожие / альтернативные» на карточке (атрибутные дистанции без ML)

### Фаза 6 — Полировка демо
- [ ] Пустые состояния,loading, ошибки
- [ ] SEO-базис: метатеги, sitemap, SSR каталога
- [ ] Сквозная проверка сценариев, чистка

**Сознательно не делаем в демо:** платежи, юрлица с ролями, комиссионная отчётность, полноценный 1С-обмен, Meilisearch. Все ложатся на текущую модель без миграционной боли.

## 5. Открытые вопросы

- [ ] AI-провайдер и ключ API (пока заглушка)
- [ ] Бренды/прайсы реальных селлеров для реалистичного сида
- [ ] Точный акцентный цвет/стиль UI (соберём UI-кит в фазе 1)

## 6. Журнал работы

- **2026-09-15** — Зафиксирован стек (Next.js full-stack + сервисный слой + Prisma/Postgres, поиск поэтапно Postgres→Meilisearch). Создан каркас Next.js 16 + TS + Tailwind v4. Референс UI поиска — DNS-маркетплейс.
- **2026-09-15** — Фаза 0 завершена: Prisma-схема (User/Seller/Category/Product/Offer/Order + статусы модерации), номенклатура 5 корневых / 18 листовых категорий со схемами атрибутов, docker-compose (Postgres 16), seed 224 товара / 5 селлеров / демо-заказ. Сборка проходит.
  - Откатились с prisma@8.0.0-rc на стабильный prisma@^6 (RC с новым CLI не подходит под принцип «проверенные технологии»).
  - Нативный Postgres на хосте занимает 5432 → контейнер наружу на **5433** (DATABASE_URL в .env).
  - Seed детерминированный (mulberry32), логины демо-аккаунтов печатает в конце (`npm run db:seed`).
  - Демо-аккаунты: `admin@demo.ru/admin123`, `customer@demo.ru/customer123`, селлеры `nordtool@demo.ru` … `stalstandart@demo.ru`, пароль `seller123`.
- **2026-09-15** — Фаза 1 завершена: витрина целиком на server components + URL-состоянии фильтров (`src/lib/catalog-url.ts` — чистые мутаторы состояния, шарятся между сервером и клиентом). Сервис каталога `src/services/catalog.service.ts`: сырой SQL с JSONB-условиями (`attributes->>'code'`), фасетные счётчики считаются в JS по базовой выборке категории (демо-объёмы; при росте — Meilisearch). Вид списка — DNS-стиль: строка с ключевыми характеристиками, цена/наличие справа. Корзина сознательно не показана (фаза 2). Сид усилен: 328 товаров, ключевые атрибуты заполняются всегда.
  - Из заметного: resolveCategoryChain изначально включал саму категорию в предков → 404 на вложенных путях (исправлено).
- **2026-09-15** — Фаза 2 завершена: корзина (клиентское состояние [{offerId, qty}] в localStorage, всё остальное — с сервера, ценам клиента не доверяем), заказ-заявка без оплаты (гостевая и авторизованная), кабинет покупателя, аутентификация demo (scrypt + jose JWT, формат паролей совместим с сидом). Остатки на этапе заявки не списываем — резервирование за рамками демо. AUTH_SECRET добавлен в .env/.env.example. Тестовый покупатель: test-buyer@demo.ru/test1234.
  - Из заметного: в списке каталога понадобился bestOfferId — добавлен подзапросом в SQL каталога и include'ом на главную.
- **2026-09-15 (уточнение процесса)** — Обнаружено, что коммиты фаз 0–2 оставались только локально: remote был настроен, но push не выполнялся. Все коммиты отправлены в origin/main; дальше push — обязательная часть завершения фазы.
