# Скопируйте в .env и поправьте под окружение
DATABASE_URL="postgresql://metalcut:metalcut@localhost:5433/metalcut"

# AI-слой (OpenAI-совместимый эндпоинт). Пусто = работает rule-based заглушка.
LLM_API_BASE=""
LLM_API_KEY=""
LLM_MODEL=""

# Секрет подписи сессионных JWT
AUTH_SECRET="<замените на случайную строку>"
